# Java - Workbench

This project serves as testing grounds for code, written in Java[^1].

Currently testing:
+ `Iterable` and `Iterator` interfaces
+ for-each loops

[^1]: The Code needs to be built and compiled first, as there are no binaries included.