package tinygame;

import java.awt.*;

/**
 * Implements methods that orders a class
 * to define rendering logic.
 */
public interface Renderable {
    /** Processes the logic for rendering. */
    void render(Graphics2D graphics);
}
